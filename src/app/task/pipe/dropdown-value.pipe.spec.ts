import { DropdownValuePipe } from './dropdown-value.pipe';

describe('DropdownValuePipe', () => {
  it('create an instance', () => {
    const pipe = new DropdownValuePipe();
    expect(pipe).toBeTruthy();
  });
});
